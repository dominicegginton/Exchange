doctype html5
html(lang='en')
	include includes/head.pug
	head
		link(rel = 'stylesheet', href = '/stylesheet/item')
		title='Exchange - ' + user.name
	body
		main
			include includes/banner.pug
			.item
				img(src='/item/image/' + item.image)
				h1=item.name
				p=item.description
