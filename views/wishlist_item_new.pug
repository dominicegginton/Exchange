doctype html5
html(lang='en')
	include includes/head.pug
	head
		link(rel = 'stylesheet', href = '/stylesheet/controls')
		title Exchange - New Item
	body
		main
			include includes/banner.pug
			h1 New Wish List Item
			h2 let others know what you want to trade for your item
			if error
				.error
					p=error.message
			form(method='POST', action='', enctype="multipart/form-data", autocomplete="off")
				.group
					input.textbox(type='text', name='name', required)
					label(for="name") Item Name
				.group
					textarea.textbox.textarea(name="description", required)
					label(for="description") Description
				.group
					button.button.greenButton(type='submit') Add Item To WIsh LIst
